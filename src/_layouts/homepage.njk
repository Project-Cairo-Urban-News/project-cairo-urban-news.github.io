<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- <link rel="stylesheet" href="css/styles.css"> -->
    <title>{{ title }}</title>
    <style>
      
      @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+Arabic:wght@100..900&display=swap');
      @import url('https://fonts.googleapis.com/css2?family=Fustat:wght@200..800&display=swap');

      /* Full-screen background image */
      body, html {
        height: 100%;
        margin: 0;
        font-family: Times, serif;
      }
      
      .background-image {
        background-image: url('images/chareh_abdine_1870_1945.jpg');
        background-size: cover;
        background-position: center;
        position: fixed;
        width: 100%;
        height: 100%;
        z-index: -1;
      }

      /* Dark overlay for readability */
      .overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
        z-index: 0;
      }

      /* Centered content */
      .content {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        color: white;
        text-align: center;
        z-index: 1;
      }

      /* Large text styles */
      .display-3 {
        font-size: 3rem;
      }

      .bi {
        transform: scale(3.5); 
        transform-origin: center center; 
      }

      .navbar-text.display-3 {
        display: block;
        font-family: 'Fustat';
        font-weight: 600; 
      }

      .title-container {
        display: flex;          /* Aligns both spans horizontally */
        align-items: center;    /* Vertically centers the spans */
        justify-content: center; /* Centers them within the parent container */
        gap: 20px;
        padding-top: 90px
    }
      .col-lg {
      font-family: 'Fustat';
      font-weight: 400; 
      line-height: 1.8;
      font-size: 2rem;
      margin-top: 20px;
      
    }
      .col-lg a {
      color: #FFFFFF;  /* Default color */
      text-decoration: none;  /* Remove underline */
      transition: color 0.3s ease, text-decoration 0.5s ease;  /* Transition time and effect */
    }
      /* Change color on hover */
      .col-lg a:hover {
      color: #507f93;  /* Hover color */
      text-decoration: none;  /* Underline text on hover */
    }
      .dot {
      font-size: 1.5rem;        /* Size of the dot, adjust as needed */
      margin: 10px;         /* Space on either side of the dot */
      color: white;           /* Dot color, you can change it to match the design */
    }
    </style>
  </head>
  <body>
    <div class="background-image"></div>
    <div class="overlay"></div>

    <div class="content">
      <a class="navbar-brand" href="/">
        <svg class="bi me-2" width="100" viewBox="0 0 150 100">
          <use xlink:href="#wm_logo" />
        </svg>
      </a>

      <div class="title-container">
        <span class="navbar-text display-3">News of Cairo</span>
        <span class="dot">•</span>
        <span class="navbar-text display-3">أخبار القاهرة</span>
      </div>

      <div class="container-lg pt-1 text-center" style="position: relative; z-index: 1;">
        <div class="col-lg mx-auto display-5">
          {{ content | safe }}
        </div>
      </div>
    </div>

     {% include 'wm_logo.html' %}
    <script type="module" src="js/main.js"></script>
  </body>
</html>
